https://github.com/sagartmg2/mangsir-2082


U new files
A new fiels are tracked


1. > git init
2. > git remote add origin <github-repo-url>
    eg: git remote add origin https://github.com/sagartmg2/mangsir-2082


2.1 to check if 2. worked 
    > git remote -v



<!--  every day  -->

3. > git add .   // adds all the new files and modified files

3.1 to check if 3. worked
    > git status


4. > git commit -m "html-css"

5. git push origin <branch-name>  // first check the branch you are in using > git branch

    eg: git push origin master
        git push origin main 